import type { App } from 'vue'

export function injectAppPlugins(app: App) {
  // 💬 Example: toast, analytics, UI kits
  // app.use(MyNotificationPlugin)
  // app.config.globalProperties.$log = customLogger
}